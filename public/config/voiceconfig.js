import { TTSPlayer } from '../actions/audioexample.js';
const VoiceControl = document.getElementById('VoiceControl');
VoiceControl.addEventListener('click', () => {
    /* const newAiForm = initcommandForm('translateAPIFormContainer');
    const newAiForm = initVoiceForm('voiceFormContainer');
    }); */
});